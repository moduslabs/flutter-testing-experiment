name: flutter_testing_experiment
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  lint: ^1.6.0
  stacked: ^2.2.5
  stacked_services: ^0.8.13

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.15
  stacked_generator: ^0.5.2
  build_runner: ^2.1.2

flutter:
  uses-material-design: true